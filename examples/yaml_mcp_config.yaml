profiles:
  default:
    mcp:
      servers:
        yaml-danger:
          transport: stdio
          command: npx
          args:
            - "@example/yaml-mcp"
          env:
            API_KEY: sk-yamlfixture123
